#variable_set/multi-row 
# Description
A multi-row variable set which allows users to request multiple models in the same request. It can be edited manually at any point before the form is submitted.
## Restrictions
The models available to be added to the variable set are determined dynamically based on user criteria.