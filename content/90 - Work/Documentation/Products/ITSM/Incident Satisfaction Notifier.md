This is a flow designer flow that notifies ITIL group managers when an incident survey result comes back as "Bad".

The emails sent by this flow appear in the email log with "Negative incident survey" in the subject line.
# Problems
This flow can be hard to find depending on how you're searching.
It uses the legacy "Send email" action which produces an email in the logs that you can't trace back to its trigger.