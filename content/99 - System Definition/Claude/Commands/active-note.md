Read the active note and use it as the primary context for the following prompt. Use the rest of the vault for supporting context where it's relevant — structure, tags, linked notes, templates, existing patterns — but the active note is the main subject.

Steps:
1. Run `obsidian file active` to get the active note's path
2. Run `obsidian read` on that file to get its full contents
3. Execute the user's prompt with that note as the focal point

User's prompt: $ARGUMENTS
